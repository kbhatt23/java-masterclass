package basics;
@FunctionalInterface
public interface SingleAbstractInterface {

	public void task();
	
	//objecty class methods can also be abstract and still functional in nature
	public boolean equals(Object o);
}
